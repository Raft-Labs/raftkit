# Fixture — sentence over 25 words, wrapped right before a capitalized word

```output
This deliberately long test sentence about the plain language checker wraps right before a
API acronym so the join logic must not depend on the next line starting lowercase to catch it.
```
