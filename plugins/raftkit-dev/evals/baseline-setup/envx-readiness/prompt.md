I just cloned this project and I'm trying to get it running locally. I noticed the repo has a bunch of `.env.development.gpg` and `.env.production.gpg` files instead of plain env files, plus an `.envxrc` config at the root. Can you get my local environment set up so I can actually run the app? I want to make sure I can decrypt and use these env files as part of the setup, not find out later that something's missing.
