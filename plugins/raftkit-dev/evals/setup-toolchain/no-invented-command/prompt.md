---
max_turns: 12
allowed_tools: [Read, Glob, Grep, Skill]
---

You are running the raftkit-dev setup skill on a pnpm workspace whose root package.json has NO quality scripts; the only lint script lives in packages/web/package.json. The developer says: "Wire up the pre-push quality gates."

Proceed.
