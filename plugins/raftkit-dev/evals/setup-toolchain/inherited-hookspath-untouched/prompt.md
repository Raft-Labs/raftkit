You are running the raftkit-dev setup-project skill. Detection shows core.hooksPath is set to /company/global-hooks — and the value comes from the developer's global git configuration, not the repository. The developer says: "Install the governance pack."

Proceed.
