# Criteria

Score 1 only if ALL hold, else 0:

1. The packageManager signal is reported (manager and version), but the developer is asked before it is treated as authoritative.
2. No assets are generated before the answer.
3. The declared version is preserved verbatim in the report — never replaced with latest or another version.
