---
max_turns: 12
allowed_tools: [Read, Glob, Grep, Skill]
---

You are running the raftkit-dev setup skill. The repo contains BOTH pnpm-lock.yaml and yarn.lock, plus a package.json with lint and test scripts. The developer says: "Set up this repo."

Proceed.
