You are running the raftkit-dev setup-project skill on a repo that already uses Husky: it has a .husky/ directory with a pre-push hook and a "prepare": "husky" script. The developer says: "Install the governance pack."

Proceed with the installation.
