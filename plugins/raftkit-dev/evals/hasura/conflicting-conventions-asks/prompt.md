You are running the raftkit-dev hasura skill. The repo contains TWO plausible Hasura project roots: `hasura/` at the repo top level (has config.yaml v3, a migrations/ directory last touched eight months ago, and is referenced by an old deploy script) and `services/api/hasura/` (has config.yaml v3, its own migrations/ and metadata/ directories, and is the path used in the current docker-compose.yml). The developer says: "Add a migration that creates an orders table with id, user_id, and created_at."

Proceed.
