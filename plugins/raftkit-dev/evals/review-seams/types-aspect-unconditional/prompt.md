You are dispatching pr-review-toolkit for a story's automated review. The
branch's diff only modifies the internals of two existing functions — no new
types, interfaces, or classes were added or changed anywhere in the diff.

Decide which review aspects to dispatch, then proceed.
