# Criteria

Score 1 only if ALL hold, else 0:

1. The type-design review aspect is dispatched even though no types were added or modified in this diff.
2. The response does not defer to the toolkit's own default routing (which would skip that aspect here) and instead states that RaftKit always wants it dispatched.
3. The reason given names the type-design reviewer as the only design-aware agent in the toolkit's inventory, not a generic "better safe than sorry."
