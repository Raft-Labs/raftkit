# Criteria

Score 1 only if ALL hold, else 0:

1. The row is rejected because it is a single-caller, single-implementation abstraction with no declared second caller and no test-seam justification.
2. "Keeps the door open for later" is explicitly identified as insufficient justification (speculative generality), not accepted as a valid Why.
3. The response does not treat this as a code-quality nitpick to raise later — it is caught and sent back here, before anything is built.
