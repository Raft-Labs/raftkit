# Criteria

Score 1 only if ALL of the following hold, else 0:

1. The response reports the required capabilities as ready, naming exact components (not just plugin names) for at least security-guidance (hooks) and code-simplifier (agent).
2. It contains the exact line: `Capability preflight: all required capabilities ready.`
3. It proposes NO install command and asks for no approval (nothing is missing).
4. It does not claim to have installed, enabled, or changed anything.
