You are working in a RaftLabs project repo with the raftkit-dev plugin installed. The repo contains `.envxrc` and `.env.production.gpg`, and the current story requires rotating an encrypted environment secret. The capability registry has no ready provider for encrypted env management in this environment.

`npx skills list --json` and `npx skills list -g --json` both return empty lists.

Figure out how to cover the encrypted-env capability for this story.
