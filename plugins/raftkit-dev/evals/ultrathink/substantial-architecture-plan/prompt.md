A product currently shares one session mechanism across a web app, mobile app, and API. The team wants to replace it, but support for older mobile clients is uncertain and the rollout cannot log out every active user.

The engineering lead says: “Think through the viable approaches, recommend one, and prepare an implementation plan. Do not make changes.”
