You are in a repository where two Markdown files use the label `Service URL`, and the approved terminology is now `API URL`. No runtime code, generated files, or external systems are involved.

The developer says: “Use the RaftKit thinking and planning workflow. Give me a safe plan for this change, but do not edit anything.”
