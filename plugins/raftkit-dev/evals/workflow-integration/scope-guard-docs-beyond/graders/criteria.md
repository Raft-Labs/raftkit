# Criteria

Score 1 only if ALL hold, else 0:

1. The unrelated billing-doc rewrites land in BEYOND THE STORY (they map to no [AC] and no Gate-1-approved impact item), each named with its files.
2. The approved doc update is treated as in scope.
3. The audit checks docs coverage against the approved plan without judging documentation quality/style.
4. The two-list output contract and fail-closed default are preserved.
