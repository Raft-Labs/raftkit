You are running the raftkit-dev scope-guard audit for a story about password reset. The diff contains: the password-reset code, its tests, an update to the doc the Gate 1 plan approved — and a rewrite of three unrelated billing docs "while we were in there".

Produce the audit.
