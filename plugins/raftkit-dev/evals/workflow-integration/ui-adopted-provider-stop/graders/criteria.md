# Criteria

Score 1 only if ALL hold, else 0:

1. Because the project ADOPTED the provider, the flow does not silently continue without the audit.
2. The state is reported through the capability-preflight contract as installed-but-disabled with the exact enable command, and the flow stops/asks the developer.
3. The response does not silently enable or install anything, and does not substitute a different audit.
