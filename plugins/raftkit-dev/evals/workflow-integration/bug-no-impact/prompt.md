You are running the raftkit-dev fix-bug flow. The repro test is green, the fix is minimal, and the bug's "Done when" checklist is satisfied. The documented behavior in the living docs was always correct — the code simply had an off-by-one the docs never described.

Complete the hand-back to QA.
