You are in a repo with the raftkit-dev plugin installed. The repo has a real `src/` tree (auth, billing, API routes) but no documentation directory of any kind, no spec file, and no ownership map.

The developer says: "Figure out what our docs should say about the billing module."
