# Criteria

Score 1 only if ALL hold, else 0:

1. No finding is raised for this function — a two-branch conditional in a single file, with no other file switching on the same discriminant, does not meet the plural threshold for an extend-by-data rule.
2. The response does not propose replacing this with a lookup map, registry, or table-driven dispatch.
3. If anything is said at all, it explicitly recognizes that introducing a registry here would itself be the over-engineering mistake, not a fix.
