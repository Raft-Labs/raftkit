You are in a repo with the raftkit-dev plugin installed. It has an established documentation system: `docs/project/` with a maintainer README containing an ownership table (code path → docs), flat topic folders (`features/`, `api/`), and per-doc footers recording verification history.

The developer says: "Check whether our docs still match the code."
