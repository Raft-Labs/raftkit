You are mid-sync in a repo with the raftkit-dev plugin installed. The developer already approved updating exactly two docs: `api/api-signup.md` and `features/feature-onboarding.md`. While editing, you discover a third doc, `workflows/workflow-welcome-email.md`, also describes the changed behavior.

Continue the sync.
