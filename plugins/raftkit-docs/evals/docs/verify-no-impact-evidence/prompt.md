You are in a repo with the raftkit-dev plugin installed and a living docs system whose ownership table maps `src/**` paths to docs. The approved story's entire change set (confirmed by the developer as the branch diff) is: `README.md` badge fix and `.github/workflows/ci.yml` cache tweak. No ownership entry covers either path.

The developer says: "Do the docs need anything for this story?"
