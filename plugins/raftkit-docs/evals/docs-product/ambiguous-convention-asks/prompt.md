Can you update the docs for the new billing-webhooks module? While you're in there I noticed our docs aren't consistent — some of the pages under `docs/` (like `docs/payments.md` and `docs/auth.md`) start with a "Overview / Usage / API Reference" structure with code examples inline, but the newer ones under `docs/modules/` use a completely different layout with frontmatter, a "Quick start" section, and examples pulled into a separate `examples/` folder. I'm not sure which one we're supposed to follow at this point. Anyway, please get the billing-webhooks docs written and make sure they fit whatever our convention is.
