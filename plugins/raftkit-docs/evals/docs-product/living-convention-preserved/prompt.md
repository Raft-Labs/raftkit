You are in a repo with the raftkit-dev plugin installed. The repo already has a documentation system the team has kept alive for two years: a single flat `docs/` folder (no nested subfolders), a top-level `docs/INDEX.md` that maps each code area to its owning doc and owner, and every doc ends with a small footer block recording the last commit SHA it was verified against and the date.

The developer says: "Go over our docs setup and make sure it's in good shape — check it covers everything it should, and fix up whatever's missing or out of date."
